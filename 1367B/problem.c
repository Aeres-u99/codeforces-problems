#include <stdio.h>
int main () {
    int cases, number, arr[1000];
    int i,j,k;
    scanf("%d", &cases);
    while (cases) {
        scanf("%d", &number);
        arr[number];
        do {
            scanf("%d", &arr[i++]);
        } while (getchar() != '\n' && i < n);

    }
}
